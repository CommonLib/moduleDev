<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.NestedScrollView android:id="@+id/info_sv_root"
                                            xmlns:android="http://schemas.android.com/apk/res/android"
                                            xmlns:app="http://schemas.android.com/apk/res-auto"
                                            android:layout_width="match_parent"
                                            android:layout_height="match_parent"
                                            android:focusable="true"
                                            app:layout_behavior="@string/appbar_scrolling_view_behavior">

    <LinearLayout
        android:id="@+id/info_ll_root"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:focusable="true"
        android:focusableInTouchMode="true"
        android:orientation="vertical"
        android:padding="@dimen/info_root_padding">

        <include layout="@layout/view_info_cn_revise"/>

        <ImageView
            android:id="@+id/info_iv_add_experience"
            android:layout_width="@dimen/width_228"
            android:layout_height="@dimen/height_55"
            android:layout_gravity="right"
            android:layout_marginTop="@dimen/space_20"
            android:src="@mipmap/create_profile_add_experience_zh"/>

        <include layout="@layout/item_add_comment"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_normal"
            android:text="@string/info_rights"/>

    </LinearLayout>
</android.support.v4.widget.NestedScrollView>